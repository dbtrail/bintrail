# Credentials — never commit
deploy/.env

# Build artifacts (compiled binaries at repo root and cross-compilation output)
/bintrail
/bintrail-mcp
/dist/

# Test coverage
cover.out
/tmp/bintrail-cover/

# macOS
.DS_Store
